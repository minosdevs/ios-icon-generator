# Duplique ce fichier en .env (a la racine du repo) et colle TA cle.
# .env est dans .gitignore : elle ne sera JAMAIS commit.
# Une seule des deux cles suffit ; mets les deux pour comparer les deux rendus.

OPENAI_API_KEY=
GEMINI_API_KEY=
